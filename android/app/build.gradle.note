android {
    namespace = "com.hydrobuddy.app"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.hydrobuddy.app"
        minSdk = 23
        targetSdk = 34
        versionCode = 1
        versionName = "1.0.0"
    }
}

# Play Store checklist:
# 1. App icon: flutter_launcher_icons
# 2. Signing: key.properties + `flutter build appbundle --release`
# 3. Target API 34+, 64-bit, adaptive icon, feature graphic 1024x500
# 4. Privacy policy URL required (health app) + Data safety form:
#    - collected: approximate hydration logs on-device only, no upload
# 5. POST_NOTIFICATIONS runtime prompt (handled by plugin v17)
